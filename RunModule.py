#from module import divide  #모듈에서 디바이드만 불러오겠다. 
#import module as mod #불러온 파일을 mod로 부른다.
from module import * # 함수를 쓰는데 출처가 불분명하다.그리고 __all__ 로 *쳤을때 불러오고 싶은함수 지정할 수 있거든? 근데 거기에 니가 원하는 함수가 없을 수도 있다.이마이야르 이마이야르반응
#print(divde(5, 2))
#print(mod.divide(5, 2))
from jiwong import name
name()
